import 'package:flutter/material.dart';

class TablePetaPage extends StatelessWidget {
  const TablePetaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Table Peta')),
      body: const Center(child: Text('Table Peta Page')),
    );
  }
}
